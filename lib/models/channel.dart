import 'package:flutter/material.dart';

class Channel{
  final String name, logo, link;

  Channel(
    {
      @required this.name,
      @required this.logo,
      @required this.link
    }
  );
  
}